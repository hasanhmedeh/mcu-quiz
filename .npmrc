# pnpm configuration
# Next.js and firebase-admin both resolve fine under pnpm's strict node_modules
# layout, so no hoisting workarounds are needed.
strict-peer-dependencies=false
auto-install-peers=true
# Build scripts are opt-in under pnpm 10+; these are the ones this project needs.
enable-pre-post-scripts=true
